搭建一个前端项目，目标是学习与链上合约交互，会有各种类型的合约，其对应的页面资源应各自独立。

技术栈：Next.js、TypeScript、Tailwind CSS 4、Viem v2
现在为 TokenBank合约 搭建一个简单的前端：
1. 连接钱包
2. 钱包连接后显示当前 Token 的余额，并且可以存款(点击按钮存款)到 TokenBank
3. 存款后显示用户存款金额。
4. 支持用户取款(点击按钮取款)。
5. 授权后显示当前授权额度。